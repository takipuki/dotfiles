#+OPTIONS: toc:nil
#+OPTIONS: num:0

#+OPTIONS: html-postamble:nil
#+OPTIONS: html-style:nil
#+HTML_HEAD: <link rel="stylesheet" type="text/css" href="clean.css"/>
#+HTML_DOCTYPE: html5

#+LATEX_CLASS: article
#+LATEX_CLASS_OPTIONS: [12pt]
#+LATEX_HEADER: \usepackage[a4paper, margin=1in]{geometry}

#+title: Fundamental Calculus

* Functions

A /function/ is a relation between an independent and a dependent
variable. The set of all possible values that a /function/ can take is
its /domain/ and for every such value in the domain the function must
give /exactly one/ real number in return; the set of all such outputs
is the /range/ of the function.

For example, consider the following function.
    $$ y = \frac{1}{\sqrt{1-x}} $$
$y$ is undefined when $x = 1$ and imaginary when $x > 1$ \\
\therefore the domain of the function is $(-\infty, 1)$

\begin{align*}
    y^2   &= \frac{1}{1-x} \\
    1 - x &= \frac{1}{y^2} \\
    x     &= 1 - \frac{1}{y^2}
\end{align*}
So there are no such value of $x$ for which $y$ will be 0 \\
\therefore the range of the function is $\mathbb{R} - \{0\}$

1. 
   b. [@b] $y = \sqrt[^3]{2x-1} - 3$ \\
      not a function; undefined for $x = 0$
   c. $|y| = 2x$ \\
      not a function; for $x = 1$, $y = 2, -2$
   d. $y = \sqrt{5-x^2} + 3$ \\
      not a function; undefined for $x = 3$
2.
   #+LATEX: \renewcommand{\labelenumii}{\arabic{enumii}.}
    1. D $= (-\infty, \infty)$ and R $= \{2\}$
    2. vertical line test fails \\
       D $= [-1, 3]$ and R $= [-2,3]$
    3. D $= (-\infty, \infty)$ and R $= (-\infty, \infty)$
    4. vertical line test fails \\
       D $= [-3,4)$ and R $= [-1,1]$
    5. D $= [1,4]$ and R $= [-4,4]$
    6. vertical line test fails \\
       D $= [-3,3]$ and R $= [-3,3]$
    7. D $= [4,4]$ and R $= [-4,-4]$
    8. vertical line test fails \\
       D $= (-4, 3]$ and R $= (-2, 2)$
    9. D $= [-5,5]$ and R $= [1,3]$
