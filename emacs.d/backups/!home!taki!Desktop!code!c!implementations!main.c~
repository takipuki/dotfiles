#include <stdio.h>

int main(void) {
    int m; scanf("%d", &m);
    int a[m];
    for (int i = 0; i < m; i++)
        scanf("%d", a+i);

    int n; scanf("%d", &n);
    int b[n];
    for (int i = 0; i < n; i++)
        scanf("%d", b+i);

    int c[m+n];
    for (int a_i = 0, b_i = 0, c_i = 0; c_i < m+n; c_i++) {
        if (a[a_i] < b[b_i])
            c[c_i] = a[a_i++];
        else
    }
}
